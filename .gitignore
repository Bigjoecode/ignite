# Server secrets (CMS config, DB credentials) — repo is public
.env
config.local.php
*.sql
backups/
# Form submissions, CMS database and sessions — local dev copy of the server data dir
ignite-data/
# Media library uploads (live on the server only; deploys never touch them)
public/uploads/
.DS_Store
Thumbs.db
