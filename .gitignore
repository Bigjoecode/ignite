# Server secrets (CMS config, DB credentials) — repo is public
.env
config.local.php
*.sql
backups/
# Form submissions (patient contact details) — local dev copy of the server data dir
ignite-data/
.DS_Store
Thumbs.db
